# Kotlin Daily Tips 목차

## 선정 이유
이 목차는 Java 개발자가 Kotlin으로 전환할 때 가장 유용하고 흥미로운 기능들을 중심으로 구성했습니다. 각 팁은 간결하고 실용적인 내용으로 구성하여 매일 하나씩 학습하기 적합하도록 했습니다. 특히 Java와 다르게 동작하거나 Java에는 없는 기능들을 강조했습니다.

## 목차

### 기본 문법과 타입 시스템
1. **Null 안전성: 안전 호출 연산자 `?.`** - NPE를 방지하는 안전한 메서드 호출
2. **Null 안전성: Elvis 연산자 `?:`** - null 대체값을 제공하는 간결한 방법
3. **Null 안전성: 단언 연산자 `!!`** - null이 아님을 컴파일러에게 알리는 방법
4. **Val vs Var** - 불변성과 가변성의 명시적 구분
5. **스마트 캐스트** - 타입 검사 후 자동 캐스팅의 마법
6. **문자열 템플릿** - 문자열 내 변수와 표현식 사용하기
7. **Raw 문자열** - 이스케이프 문자 없이 여러 줄 문자열 작성하기
8. **기본 타입 확장: 숫자 함수** - Int, Double 등에 추가된 유용한 함수들
9. **기본 타입 확장: 문자열 함수** - String에 추가된 강력한 함수들
10. **타입 별칭(typealias)** - 복잡한 타입에 별칭 부여하기

### 함수와 람다
11. **함수 기본값 매개변수** - 선택적 인자를 위한 기본값 설정
12. **명명된 인자(Named Arguments)** - 함수 호출 시 매개변수 이름 지정하기
13. **단일 표현식 함수** - 간결한 함수 정의 방법
14. **로컬 함수** - 함수 내부에 함수 정의하기
15. **람다 표현식 기초** - 코틀린 람다의 간결한 문법
16. **it 키워드** - 단일 매개변수 람다에서의 암시적 파라미터
17. **함수 참조(::)** - 기존 함수를 람다처럼 전달하기
18. **고차 함수** - 함수를 인자로 받거나 반환하는 함수
19. **인라인 함수** - 람다의 오버헤드를 제거하는 최적화
20. **후행 람다 문법** - 함수의 마지막 인자가 람다일 때의 특별한 문법

### 컬렉션과 함수형 프로그래밍
21. **불변/가변 컬렉션** - listOf vs mutableListOf의 차이
22. **컬렉션 생성 함수** - 다양한 컬렉션 초기화 방법
23. **filter 함수** - 조건에 맞는 요소만 선택하기
24. **map 함수** - 컬렉션의 각 요소 변환하기
25. **flatMap 함수** - 중첩 컬렉션 평탄화하기
26. **fold와 reduce** - 컬렉션을 단일 값으로 집계하기
27. **groupBy 함수** - 요소를 그룹화하기
28. **시퀀스 기초** - 지연 평가 컬렉션 연산
29. **시퀀스 vs 즉시 연산** - 성능 차이 이해하기
30. **컬렉션 확장 함수 만들기** - 나만의 컬렉션 유틸리티 함수 작성

### 스코프 함수
31. **let 함수** - null 체크와 결과 변환을 한번에
32. **apply 함수** - 객체 초기화와 설정을 간결하게
33. **run 함수** - 객체 컨텍스트에서 여러 연산 수행하기
34. **with 함수** - 수신자 객체 없이 속성과 함수 접근하기
35. **also 함수** - 객체를 수정하면서 체이닝하기
36. **스코프 함수 선택 가이드** - 언제 어떤 스코프 함수를 사용할지 결정하기

### 클래스와 객체
37. **주 생성자와 부 생성자** - 클래스 초기화의 두 가지 방법
38. **데이터 클래스** - equals, hashCode, toString 자동 생성
39. **데이터 클래스: copy 함수** - 불변 객체의 변형 만들기
40. **객체 선언(object)** - 싱글톤 패턴의 간결한 구현
41. **동반 객체(companion object)** - 자바 정적 멤버의 코틀린 대안
42. **중첩 클래스 vs 내부 클래스** - static과 inner의 차이
43. **봉인된 클래스(sealed class)** - 제한된 클래스 계층 정의하기
44. **열거형 클래스** - Java enum보다 강력한 코틀린 enum
45. **위임 패턴(by)** - 상속 없이 기능 재사용하기
46. **프로퍼티 위임** - lazy, observable 등 특수 프로퍼티 구현

### 확장 기능
47. **확장 함수 기초** - 기존 클래스에 새 메서드 추가하기
48. **확장 프로퍼티** - 기존 클래스에 새 프로퍼티 추가하기
49. **수신자 타입 제한** - 특정 타입에만 확장 함수 적용하기
50. **널러블 타입 확장** - null 가능 타입에 대한 확장 함수
51. **연산자 오버로딩** - 사용자 정의 클래스에 연산자 기능 추가하기
52. **중위 함수(infix)** - 더 자연스러운 함수 호출 문법
53. **인라인 클래스** - 래퍼 오버헤드 없는 타입 안전성

### 코루틴 기초
54. **코루틴 스코프** - 코루틴의 생명주기 관리하기
55. **launch vs async** - 코루틴 빌더의 차이점
56. **코루틴 컨텍스트** - 코루틴 실행 환경 설정하기
57. **코루틴 디스패처** - 코루틴이 실행될 스레드 지정하기
58. **구조화된 동시성** - 부모-자식 코루틴 관계 이해하기
59. **코루틴 취소** - 실행 중인 코루틴 안전하게 중단하기
60. **코루틴 예외 처리** - try-catch와 예외 전파 이해하기

### 코루틴 고급
61. **Flow 기초** - 비동기 데이터 스트림 처리하기
62. **Flow 연산자** - map, filter 등으로 Flow 변환하기
63. **Flow 컬렉션** - Flow에서 값 수집하기
64. **Flow 컨텍스트** - Flow의 실행 컨텍스트 관리
65. **채널 기초** - 코루틴 간 통신하기
66. **Select 표현식** - 여러 비동기 작업 중 첫 번째 완료 기다리기

### 상호운용성과 도구
67. **Java에서 코틀린 코드 호출하기** - 자바 개발자를 위한 API 설계
68. **코틀린에서 Java 코드 호출하기** - 플랫폼 타입과 널 안전성
69. **@JvmStatic과 @JvmField** - 자바 친화적인 코틀린 코드 작성
70. **KAPT 소개** - 코틀린 어노테이션 처리 도구
71. **KSP 소개** - 더 빠른 코틀린 심볼 처리
72. **KAPT vs KSP** - 어떤 것을 선택해야 할까?
73. **AllOpen 플러그인** - final 클래스 제약 우회하기
74. **NoArg 플러그인** - 인자 없는 생성자 자동 생성하기
75. **코틀린 스크립트(.kts)** - 스크립팅 용도로 코틀린 사용하기

### 실용적인 팁
76. **Spring Boot에서 코틀린 사용하기** - 스프링 프레임워크 통합
77. **코틀린으로 테스트 코드 작성하기** - JUnit과 함께 사용
78. **코틀린 DSL 만들기** - 도메인 특화 언어 설계
79. **멀티플랫폼 프로젝트 소개** - 코드 재사용의 미래
80. **코틀린 1.5/1.6 주요 신기능** - 최신 언어 기능 활용하기
